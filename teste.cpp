#include <iostream>
using std::cout;
using std::cin;
using std::endl;

#include <algorithm>
using std::copy;

#include <vector>
using std::vector;

#include <cassert>

#include <cmath>

#include <string>
using std::string;

int main()
{
    for(int i = 0; i < n; i++)
    {
        for(int j = 0; j < m; j++)
        {
            for(int l = 0; l < 4; l++)
            {
                if(l < n)
            }
        }
    }
}